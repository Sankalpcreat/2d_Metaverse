import React from 'react';
import Signup from '../auth/Signup';  // Adjust path if necessary

const SignupPage: React.FC = () => {
  return (
    <div>
      <h2>Create a New Account</h2>
      <Signup />
    </div>
  );
};

export default SignupPage;
